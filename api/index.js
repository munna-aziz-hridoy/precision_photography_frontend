import { login, getUser } from "./auth";
import { getRealtors, addRealtors, editRealtors } from "./realtors";
import { getAreas, addArea, editArea, getArea } from "./area";
import {
  getCommunities,
  addCommunity,
  editCommunity,
  getCommunity,
} from "./community";
import {
  getGalleries,
  getGalleryDetails,
  addGallery,
  addImageToGallery,
  applyManualSort,
  applySortByName,
} from "./gallery";

export {
  login,
  addRealtors,
  getUser,
  getRealtors,
  editRealtors,
  getAreas,
  addArea,
  editArea,
  getCommunities,
  addCommunity,
  editCommunity,
  getGalleries,
  getGalleryDetails,
  addGallery,
  addImageToGallery,
  getArea,
  applyManualSort,
  applySortByName,
  getCommunity,
};
